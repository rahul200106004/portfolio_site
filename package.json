{
  "name": "portfolio-api",
  "version": "1.0.0",
  "description": "Portfolio API serverless functions",
  "main": "api/health.js",
  "dependencies": {
    "axios": "^1.8.4"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm install && npm run build"
  }
}
